apiVersion: v2
# Chart name is the product name. The RENDERED identity is decoupled from it via
# .Values.nameOverride (defaulted to "exchange" in values.yaml) because
# app.kubernetes.io/name feeds spec.selector, which is immutable on live objects.
name: finora-ex
description: Reusable Helm chart for the Finora Exchange stack (backend api/wss, frontend, admin, cms, nuxt, celery workers, in-cluster bitcoind)
type: application
version: 0.4.0
appVersion: "1.0.0"
keywords:
  - exchange
  - finora
  - gitops
maintainers:
  - name: DevOps
