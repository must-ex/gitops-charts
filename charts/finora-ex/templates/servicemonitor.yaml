{{- if .Values.monitoring.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "finora-ex.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "finora-ex.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "finora-ex.selectorLabels" . | nindent 6 }}
  endpoints:
    - path: {{ .Values.monitoring.metricsPath }}
      interval: {{ .Values.monitoring.scrapeInterval }}
      scheme: http
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}
{{- end }}
