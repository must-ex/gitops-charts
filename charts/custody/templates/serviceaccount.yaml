{{/*
ServiceAccounts:
  - chart-level SA: when .Values.serviceAccount.create is true. Uses .Values.global.esoRoleArn (legacy single-IRSA pattern, kept for parity with finora-ex/finora-sto charts).
  - per-service SA: when a service has .serviceAccount.create=true. Allows a distinct IRSA role per service (custody-core uses this for the S3/media IRSA role).
*/}}
{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "custody.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "custody.labels" . | nindent 4 }}
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.global.esoRoleArn }}
{{- end }}
{{- range $name, $svc := .Values.services }}
{{- if and $svc.enabled $svc.serviceAccount $svc.serviceAccount.create }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ $svc.serviceAccount.name | default $name }}
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "custody.labels" $ | nindent 4 }}
    app.kubernetes.io/component: {{ $name }}
  {{- with $svc.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
